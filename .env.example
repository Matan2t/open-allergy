# Only needed to enable physical card orders (set these on the Cloudflare
# Pages project, or in .dev.vars for local `wrangler pages dev`).
# The site works fully without them; the order page shows "not enabled yet".
STRIPE_SECRET_KEY=sk_test_...
CARD_PRICE_CENTS=900
SITE_URL=https://openallergycards.pages.dev
